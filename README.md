
# 🚀 Finance Viewer

**Finance Viewer** — это веб-приложение для анализа и отслеживания криптовалют в реальном времени.
Проект состоит из **фронтенда (React + Next.js)** и **бэкенда (.NET / ASP.NET Core)**.

---

## 📌 Основные возможности

* Просмотр рынка криптовалют (цена, капитализация, объем, 24h High/Low)
* Сортировка криптовалют по разным параметрам
* Линейный график изменения цены в реальном времени (Binance WebSocket)
* Получение баланса криптовалют (BTC, ETH, SOL) по адресу кошелька
* Работа через API-прокси для обхода CORS и ngrok-ограничений

---

## 🧩 Технологический стек

### Frontend

* React
* Next.js
* TailwindCSS
* Chart.js (`react-chartjs-2`)
* WebSocket (Binance)

### Backend

* ASP.NET Core (.NET)
* Entity Framework Core
* SQLite
* ASP.NET Identity

---

## 📂 Структура проекта

```
BestCryptoProject/
├─ FinanceViewer-master/
│  └─ FinanceViewer/
│     └─ Program.cs        ← Backend (.NET)
│
├─ FinanceViewerFrontend2-main/           ← Frontend (React + Next.js)
```

---

## ⚙️ Запуск проекта локально

### ✅ Предварительные требования

Убедитесь, что у вас установлены:

* **Node.js** (v18+ рекомендуется)
* **npm**
* **.NET SDK 7.0+**
* **Git**

---

## 🔧 Запуск Backend (.NET)

1. Перейдите в папку с бэкендом:

   ```bash
   cd BestCryptoProject/FinanceViewer-master/FinanceViewer
   ```

2. Запустите проект:

   ```bash
   dotnet run
   ```

3. Бэкенд будет доступен по адресу (пример):

   ```
   https://localhost:5000
   ```

> ⚠️ Убедитесь, что строка подключения к SQLite указана корректно в `appsettings.json`.

---

## 🎨 Запуск Frontend (React + Next.js)

1. Перейдите в папку фронтенда:

   ```bash
   cd FinanceViewerFrontend2-main
   ```

2. Установите зависимости:

   ```bash
   npm install
   ```

3. Создайте файл `.env.local` и укажите URL бэкенда:

   ```env
   NEXT_PUBLIC_API_BASE_URL=https://localhost:5001/api/v1
   ```

4. Запустите сервер разработки:

   ```bash
   npm run dev
   ```

5. Откройте в браузере:

   ```
   http://localhost:3000
   ```

---

## 🔁 API Proxy и CORS

Для обхода CORS и предупреждений ngrok используется **API-прокси** на стороне Next.js.
Он добавляет заголовок:

```http
ngrok-skip-browser-warning: 1
```

и проксирует запросы к бэкенду, возвращая корректный JSON.

---

## 🧪 Возможные проблемы и решения

### ❌ API возвращает HTML вместо JSON

✔️ Исправлено — API теперь всегда возвращает JSON.

### ❌ CORS / ngrok ошибка

✔️ Решено с помощью серверного API-прокси в Next.js.

### ❌ Ошибка `addCandlestickSeries is not a function`

✔️ График японских свечей заменён на линейный график (`react-chartjs-2`).

---

## 👨‍💻 Авторы проекта

- **Igor Makarenko** — Backend Developer  
  GitHub: https://github.com/Igggosha

- **Zakhar Kotov** — Frontend Developer  
  GitHub: https://github.com/GboyYouMam

- **Bogdan Kononov** — Fullstack / API & Integration  
  GitHub: https://github.com/smmlt


---

## 📈 Возможные улучшения

* Добавление графика японских свечей
* Расширение списка поддерживаемых криптовалют
* Аналитика портфеля пользователя
* Улучшение UX и производительности

